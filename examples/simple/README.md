
# Nintendo DS Package Example

- Build the ROM: `nix build`
    - ROM will be placed at `result/simple-example.nds`
- Enter development shell: `nix develop`
    - Within the shell, compile with `make`
    - ROM will be placed in this directory, as `simple-example.nds` (not in the `result/` directory)

